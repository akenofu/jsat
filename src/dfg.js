import Graph from 'digraphe';

export default class DFG {
    constructor(cfg) {
        // this.graph = new Graph();
        // cfg.cfgs.forEach(cfgManager => {
        //     cfgManager.bm.forEach(block => {
        //         this.traverse_block(block);
        //     })
        // })



    }

    //     traverse_block(block) {
    //         block.nodes.forEach(node => {
    //             if (node.type == 'VariableDeclaration') {

    //                 node.declerations.forEach(variableDeclerator => {
    //                     VariableDeclarator
    //                 })
    //             }
    //         })
    //     }
}